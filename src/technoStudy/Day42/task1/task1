Create Vehicle class with:
    instance -> move method, that accepts distance
    static -> getName method, that returns name of the class
Create Car class that's a child of Vehicle
    override and hide the method of parent